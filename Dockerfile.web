FROM ubuntu:latest

# 에이전트 웹 설치
curl -fsSL https://raw.githubusercontent.com/jmfederico/pi-web/main/install.sh | sh

# 웹 서비스 포트 노출 (예: 3000)
EXPOSE 3000

WORKDIR /app/web

# 웹 서비스 실행
CMD ["pi-web", "start"]